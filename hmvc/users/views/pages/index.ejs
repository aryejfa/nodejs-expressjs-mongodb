<!DOCTYPE html>
<html lang="en">
  <%- include('../components/header') %>
  <body>
    <% if(dataTokenJWT){ %> <%- include('../components/navbar_user') %> <%
    }else{ %> <%- include('../components/navbar') %> <% } %>
    <div
      class="mx-auto mt-8 w-full max-w-container px-4 sm:px-6 lg:px-8 text-center pt-8"
    >
      <h2>
        HOME - Node JS, Express, JWT, Aes256, Winston, Redis, No SQL (MongoDB)
      </h2>
    </div>

    <%- include('../components/footer') %>
  </body>
</html>
